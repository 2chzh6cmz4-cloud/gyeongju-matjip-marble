# 경주 맛집마블 V2.4

우상단 모서리 셀이 사라지던 문제 수정 버전입니다.

원인:
- 화면을 다시 그리는 render() 함수가 `.cell` 클래스를 전부 삭제
- 우상단의 `개인종합평가 5가 쏘기` 셀도 `.cell` 클래스를 사용해서 같이 삭제됨

수정:
- 우상단 모서리 셀을 `static-corner`로 분리
- render() 재실행 후에도 그대로 유지
- 기존 모서리 정렬 구조는 V2.3 그대로 유지

GitHub에서는 index.html과 sw.js만 교체하면 됩니다.
